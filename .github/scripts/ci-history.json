{
  "schema": 1,
  "description": "Append-only history of docs CI runs, written by .github/scripts/record_run.py. Each run lists per-page code-block results (pass if all blocks pass).",
  "runs": []
}
